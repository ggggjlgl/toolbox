export enum FuncName {
    RenameFiles,
    ExtractFiles,
    CompareClipBoard,
    PushFiles
}


export struct FuncInfo {
    name: FuncName,
    is_cur: bool,
}

export struct RenameFilesRun {
    method: string,
    path: string,
    specify_characters: string,
}
